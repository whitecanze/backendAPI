let jwt;_e34‍.x([["default",()=>_e34‍.o]]);_e34‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const getUser = token => {
    if (!token) return null
    
    // 'Bearer ....' -> [Bearer, ....]
    const parsedToken = token.split(' ')[1]
    
    try {
        const decodedToken = jwt.verify(parsedToken, process.env.SECRET)
    
        return decodedToken.userId
    } catch (error) {
        return null
    }
}

_e34‍.d(getUser);